$(document).ready(function () {
	var editor = new MediumEditor('.resume-editable', {
		disableReturn: true
	});

	//$('#sort-wrapper').sortable({ placeholder: "ui-state-highlight", helper: 'clone' });
});